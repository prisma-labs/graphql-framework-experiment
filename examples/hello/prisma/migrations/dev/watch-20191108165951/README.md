# Migration `watch-20191108165951`

This migration has been generated by Jason Kuhrt at 11/8/2019, 4:59:51 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "lift"."User" (
  "id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT ,
  "name" TEXT NOT NULL DEFAULT ''  
);
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..watch-20191108165951
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,13 @@
+datasource db {
+  provider = "sqlite"
+  url      = "file:dev.db"
+}
+
+generator photon {
+  provider = "photonjs"
+}
+
+model User {
+  id   Int    @id
+  name String
+}
```

## Photon Usage

You can use a specific Photon built for this migration (watch-20191108165951)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/watch-20191108165951'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
