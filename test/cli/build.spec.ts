import { Build } from '../../src/cli/commands/build'

describe('hello', () => {
  it('prints todo', async () => {
    // const spy = jest.spyOn(process.stdout, 'write')
    // await Build.run([])
    // expect(spy.mock.results).toMatchInlineSnapshot()
    // spy.mockRestore()
  })
})
